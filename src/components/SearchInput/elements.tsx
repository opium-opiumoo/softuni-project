import styled from "styled-components";

export const Container = styled(({...props}) => <div {...props} />)`

`

export const SearchSubmit = styled(({...props}) => <button {...props}>
    
</button>)`

`

export const SearchInput = styled(({...props}) => <input placeholder="Search" {...props} />)`
    font-size: 24px;
    padding: 15px;
    border-radius: 30px;
`